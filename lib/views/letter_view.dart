import 'package:flutter/material.dart';

class LetterView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Letter'),
      ),
      body: Center(
        child: Text('Letter View'),
      ),
    );
  }
}
